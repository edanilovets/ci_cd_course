- job:
    name: CD_for_CD/jenkins_job_builder
    project-type: freestyle
    # description: 'CD for CD job'
    builders:
      - shell: "/usr/local/bin/jenkins-jobs test -r jobs/"
    scm:
      - git:
          url: https://github.com/edanilovets/ci_cd_course.git
          branches:
            - master
